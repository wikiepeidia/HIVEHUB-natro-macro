---
phase: 03-live-flow-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - .planning/phases/03-live-flow-validation/03-MANUAL-VALIDATION.md
autonomous: true
requirements:
  - FLOW-02
must_haves:
  truths:
    - There is a repeatable manual checklist for validating the full Trading Hub cycle against the root runtime.
    - Phase output states clearly whether the live Roblox-backed run was completed or deferred.
  artifacts:
    - path: .planning/phases/03-live-flow-validation/03-MANUAL-VALIDATION.md
      provides: Manual Trading Hub validation steps and expected outcomes.
  key_links:
    - from: .planning/phases/03-live-flow-validation/03-MANUAL-VALIDATION.md
      to: submacros/natro_macro.ahk
      via: checklist references the active route, sprinkler no-op, Squares profile, and Rejoin exit behavior.
---

<objective>
Prepare the final live-cycle validation handoff for Trading Hub and make any deferred human-only validation explicit.

Purpose: This environment can prove static/runtime readiness, but a real Roblox-backed Trading Hub run still needs a concrete checklist instead of tribal knowledge or a false success claim.
Output: A manual validation checklist in the Phase 3 directory and a summary that states whether live validation happened or remains deferred.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-live-flow-validation/03-CONTEXT.md
@.planning/phases/03-live-flow-validation/03-RESEARCH.md
@.planning/phases/03-live-flow-validation/03-01-SUMMARY.md
@submacros/natro_macro.ahk
@paths/gtf-tradinghub.ahk
@settings/nm_config.ini
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Trading Hub live-validation checklist</name>
  <files>.planning/phases/03-live-flow-validation/03-MANUAL-VALIDATION.md</files>
  <read_first>submacros/natro_macro.ahk, paths/gtf-tradinghub.ahk, .planning/phases/03-live-flow-validation/03-RESEARCH.md</read_first>
  <action>Create `.planning/phases/03-live-flow-validation/03-MANUAL-VALIDATION.md` with a step-by-step checklist for a real Trading Hub run. Cover: launching the root macro, selecting Trading Hub in a gather slot, confirming unsupported Trading Hub slot controls stay disabled, starting a run, verifying travel reaches Trading Hub through the existing route, confirming no missing-image or missing-file errors appear, confirming no sprinkler-placement movement occurs for Trading Hub, confirming gather starts with the current profile, and confirming exit returns through `Rejoin()`.</action>
  <verify>grep -nE 'Trading Hub|Rejoin|sprinkler|missing-image|gtf-tradinghub' .planning/phases/03-live-flow-validation/03-MANUAL-VALIDATION.md</verify>
  <acceptance_criteria>
    - the manual validation file exists in the Phase 3 directory
    - the checklist covers launch, select, travel, gather, and exit
    - the checklist explicitly checks for missing-image errors and Trading Hub sprinkler no-op behavior
  </acceptance_criteria>
  <done>The next human validation step for Trading Hub is explicit and reproducible.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify static readiness and record the deferred live-run boundary</name>
  <files>.planning/phases/03-live-flow-validation/03-MANUAL-VALIDATION.md</files>
  <read_first>submacros/natro_macro.ahk, settings/nm_config.ini, .planning/phases/03-live-flow-validation/03-MANUAL-VALIDATION.md</read_first>
  <action>Run the final non-live checks again: syntax validation, grep for the Trading Hub `nm_setSprinkler` short-circuit, grep for `Rejoin` exit behavior, and confirm `settings\nm_config.ini` still uses the existing `FieldName1..3` gather keys. Do not fake a live Roblox-backed pass if the run was not actually executed in this environment; instead, record that the live cycle remains human-needed in the summary and final phase verification.</action>
  <verify>cmd //c "submacros\AutoHotkey64.exe /Validate /ErrorStdOut submacros\natro_macro.ahk" && grep -nF 'if (field="Trading Hub") {' submacros/natro_macro.ahk && grep -nF 'Rejoin()' submacros/natro_macro.ahk && grep -nE '^FieldName[123]=' settings/nm_config.ini</verify>
  <acceptance_criteria>
    - the root runtime still validates after the Phase 3 runtime change
    - the checklist references real current runtime behavior, not donor-only behavior
    - the plan output explicitly distinguishes static readiness from deferred human-only live validation
  </acceptance_criteria>
  <done>Phase 3 finishes with a truthful boundary: runtime readiness is validated, and any remaining live run is explicitly deferred instead of implied.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] the manual validation checklist exists and covers the full Trading Hub cycle
- [ ] cmd //c "submacros\AutoHotkey64.exe /Validate /ErrorStdOut submacros\natro_macro.ahk" exits without syntax errors
- [ ] grep confirms the Trading Hub sprinkler no-op and Rejoin exit behavior remain present
- [ ] grep confirms settings/nm_config.ini still persists gather slots through FieldName1..3
</verification>

<success_criteria>

- All tasks completed
- There is a concrete manual validation guide for Trading Hub live testing
- Static readiness is re-verified after the runtime fix
- Deferred human validation is explicit, not hidden

</success_criteria>

<output>
After completion, create `.planning/phases/03-live-flow-validation/03-02-SUMMARY.md`
</output>
