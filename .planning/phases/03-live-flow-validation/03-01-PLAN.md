---
phase: 03-live-flow-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - submacros/natro_macro.ahk
autonomous: true
requirements:
  - FLOW-02
  - SAFE-02
must_haves:
  truths:
    - Trading Hub gather setup no longer attempts unsupported sprinkler-placement movement in the root runtime.
    - The active root runtime has a proven answer for Trading Hub bitmap usage instead of a speculative asset pack.
  artifacts:
    - path: submacros/natro_macro.ahk
      provides: Trading Hub-safe sprinkler setup behavior in the root runtime.
  key_links:
    - from: submacros/natro_macro.ahk
      to: nm_setSprinkler(field, loc, dist)
      via: Trading Hub returns before sprinkler-placement movement begins.
    - from: submacros/natro_macro.ahk
      to: paths/gtf-tradinghub.ahk
      via: route audit confirms the existing root file remains the live Trading Hub travel surface.
---

<objective>
Close the last proven Trading Hub runtime gap and audit whether the root macro actually needs any Trading Hub-specific bitmap assets.

Purpose: Phase 3 should fix only the live-flow behavior that static comparison already proved is wrong, then avoid speculative asset work by auditing the active root runtime.
Output: A Trading Hub-safe `nm_setSprinkler` implementation in `submacros/natro_macro.ahk` and an audited no-speculation asset conclusion recorded in the summary.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-live-flow-validation/03-CONTEXT.md
@.planning/phases/03-live-flow-validation/03-RESEARCH.md
@.planning/phases/02-ui-and-persistence/02-02-SUMMARY.md
@submacros/natro_macro.ahk
@paths/gtf-tradinghub.ahk
@Hub file/submacros/natro_macro.ahk
@Hub file/paths/gtf-tradinghub.ahk
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the donor Trading Hub sprinkler no-op into the root runtime</name>
  <files>submacros/natro_macro.ahk</files>
  <read_first>submacros/natro_macro.ahk, Hub file/submacros/natro_macro.ahk, .planning/phases/03-live-flow-validation/03-CONTEXT.md</read_first>
  <action>Add the donor-matched `if (field="Trading Hub") { return }` short-circuit to the root `nm_setSprinkler(field, loc, dist)` function immediately after `MoveSpeedFactor` is computed and before any field-distance movement starts. Do not widen this into new UI logic, new asset logic, or broader donor copying.</action>
  <verify>cmd //c "submacros\AutoHotkey64.exe /Validate /ErrorStdOut submacros\natro_macro.ahk" && grep -nF 'if (field="Trading Hub") {' submacros/natro_macro.ahk && grep -nF 'nm_setSprinkler(field, loc, dist){' submacros/natro_macro.ahk</verify>
  <acceptance_criteria>
    - submacros/natro_macro.ahk returns early for Trading Hub inside nm_setSprinkler(field, loc, dist)
    - the Trading Hub early return appears before any sprinkler-placement movement calls for that function
    - no new Trading Hub asset references or donor-only control logic are introduced in this task
  </acceptance_criteria>
  <done>Trading Hub live gathers no longer attempt unsupported sprinkler-placement movement in the root runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Audit the active route and bitmap footprint without adding speculative assets</name>
  <files>submacros/natro_macro.ahk</files>
  <read_first>paths/gtf-tradinghub.ahk, Hub file/paths/gtf-tradinghub.ahk, .planning/phases/03-live-flow-validation/03-RESEARCH.md</read_first>
  <action>Verify the existing root `paths/gtf-tradinghub.ahk` file still matches the donor route file, then audit the active root runtime and `nm_image_assets\` tree for Trading Hub-specific bitmap lookups or Trading Hub-named assets. If none exist, keep the code unchanged and record the no-asset finding in the summary rather than creating speculative bitmap files.</action>
  <verify>md5sum paths/gtf-tradinghub.ahk "Hub file/paths/gtf-tradinghub.ahk" && ! grep -RiqE 'tradinghub|trading hub' nm_image_assets && ! grep -nE 'bitmaps\[.*trading|nm_image_assets.*trading' submacros/natro_macro.ahk</verify>
  <acceptance_criteria>
    - the root and donor gtf-tradinghub route files match exactly or any mismatch is explicitly explained in the summary
    - the audit proves whether Trading Hub-specific bitmap lookups exist in the active root runtime
    - no speculative Trading Hub asset files are added when no active lookup requires them
  </acceptance_criteria>
  <done>Phase 3 has an evidence-based answer for Trading Hub asset compatibility instead of a guessed asset pack.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd //c "submacros\AutoHotkey64.exe /Validate /ErrorStdOut submacros\natro_macro.ahk" exits without syntax errors
- [ ] grep confirms the Trading Hub early return exists in nm_setSprinkler(field, loc, dist)
- [ ] md5sum confirms the root and donor gtf-tradinghub route files match
- [ ] the asset audit confirms there are no Trading Hub-specific bitmap lookups or files requiring speculative additions
</verification>

<success_criteria>

- All tasks completed
- Trading Hub no longer attempts unsupported sprinkler placement during gather setup
- The route and asset surfaces are proven rather than guessed
- No speculative Trading Hub asset pack is introduced

</success_criteria>

<output>
After completion, create `.planning/phases/03-live-flow-validation/03-01-SUMMARY.md`
</output>
