---
phase: 02-ui-and-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - submacros/natro_macro.ahk
autonomous: true
requirements:
  - SAFE-01
  - FIELD-01
  - FIELD-02
must_haves:
  truths:
    - Selecting Trading Hub in any gather slot disables unsupported controls for that slot without disturbing other supported gather settings.
    - Persisted Trading Hub selections reload into the same guarded slot behavior after the GUI unlock/config replay path runs.
  artifacts:
    - path: submacros/natro_macro.ahk
      provides: Slot-specific Trading Hub UI guardrails and boundary validation surface.
  key_links:
    - from: submacros/natro_macro.ahk
      to: nm_FieldSelect1/2/3
      via: Trading Hub slot guards apply on selection changes and persisted config replay.
    - from: submacros/natro_macro.ahk
      to: nm_TabGatherUnLock
      via: gather unlock does not re-enable unsupported Trading Hub controls permanently.
---

<objective>
Add Trading Hub-specific UI guardrails to the root gather slots and verify the Phase 2 exposure boundary.

Purpose: Phase 2 must block unsupported Trading Hub controls safely while keeping the new main-gather UI exposure and persistence behavior stable across reloads.
Output: Slot-specific Trading Hub guard logic in submacros/natro_macro.ahk and a validation pass proving gather-only exposure.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-and-persistence/02-CONTEXT.md
@.planning/phases/02-ui-and-persistence/02-RESEARCH.md
@.planning/phases/02-ui-and-persistence/02-UI-SPEC.md
@submacros/natro_macro.ahk
@settings/nm_config.ini
@Hub file/submacros/natro_macro.ahk
</context>

<threat_model>
<trust_boundaries>
- Gather slot handlers and gather-tab unlock logic both influence which slot controls end up enabled.
- Unsupported Trading Hub controls must be blocked without breaking other fields or later slot replay paths.
</trust_boundaries>
<threat_register>
<threat>
<threat_id>T-02-02</threat_id>
<category>Elevation of Privilege</category>
<component>Trading Hub gather slot UI controls</component>
<severity>medium</severity>
<disposition>mitigate</disposition>
<mitigation_plan>Add a narrow Trading Hub slot-guard helper (or equivalent) that disables only the real root controls that are unsupported for Trading Hub, and invoke it from both selection and unlock/replay paths.</mitigation_plan>
</threat>
</threat_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add slot-specific Trading Hub guardrails to the gather UI</name>
  <files>submacros/natro_macro.ahk</files>
  <read_first>submacros/natro_macro.ahk, Hub file/submacros/natro_macro.ahk, .planning/phases/02-ui-and-persistence/02-RESEARCH.md</read_first>
  <action>Create a narrow helper or equivalent logic that checks `FieldName%index%` and, when the slot field is `Trading Hub`, disables the real root controls that are unsupported for that slot: `FieldDriftCheck%index%`, `FSL%index%Left`, `FSL%index%Right`, and `FieldSprinklerDist%index%`. Re-enable those controls for non-Trading-Hub fields according to the existing slot enablement rules. Call the guard from `nm_FieldSelect1/2/3` and from `nm_TabGatherUnLock()` (or an equivalent post-unlock replay point) so persisted Trading Hub selections reload into the correct guarded state. Do not reference donor-only control IDs that do not exist in root code.</action>
  <verify>cmd //c "submacros\AutoHotkey64.exe /Validate /ErrorStdOut submacros\natro_macro.ahk" && grep -n 'Trading Hub' submacros/natro_macro.ahk && grep -n 'FieldSprinklerDist1\|FieldDriftCheck1\|FSL1Left' submacros/natro_macro.ahk</verify>
  <acceptance_criteria>
    - submacros/natro_macro.ahk disables unsupported Trading Hub slot controls using real root control IDs only
    - submacros/natro_macro.ahk applies the Trading Hub slot guards from nm_FieldSelect1, nm_FieldSelect2, and nm_FieldSelect3
    - submacros/natro_macro.ahk reapplies or preserves the Trading Hub slot guards after gather-tab unlock/reload behavior
  </acceptance_criteria>
  <done>Trading Hub slots block unsupported drift/sprinkler controls safely during selection changes and persisted reloads.</done>
</task>

<task type="auto">
  <name>Task 2: Validate gather-only Trading Hub UI and persistence boundaries</name>
  <files>submacros/natro_macro.ahk, settings/nm_config.ini</files>
  <read_first>submacros/natro_macro.ahk, settings/nm_config.ini, .planning/phases/02-ui-and-persistence/02-RESEARCH.md</read_first>
  <action>Run targeted validation proving Phase 2 delivered only the intended exposure: validate the root script with the bundled 64-bit AutoHotkey runtime; grep for the gather-only Trading Hub selector surface and the Trading Hub guard logic; grep for `ReturnType` validation accepting `Rejoin`; confirm `MFieldList` still derives from the original `fieldnamelist`; and confirm `settings\nm_config.ini` still uses the existing `FieldName1..3` keys rather than a new Trading-Hub-only storage path. Record failed assertions in the summary instead of widening scope into planter or asset work.</action>
  <verify>cmd //c "submacros\AutoHotkey64.exe /Validate /ErrorStdOut submacros\natro_macro.ahk" && grep -n 'Trading Hub\|Rejoin\|MFieldList := \[""\]\.Push(fieldnamelist\*)' submacros/natro_macro.ahk && grep -n '^FieldName[123]=' settings/nm_config.ini</verify>
  <acceptance_criteria>
    - submacros/natro_macro.ahk exposes Trading Hub in gather UI and guard logic only
    - submacros/natro_macro.ahk keeps MFieldList and planter/manual surfaces free of Trading Hub
    - settings/nm_config.ini continues to persist gather slots through FieldName1, FieldName2, and FieldName3
  </acceptance_criteria>
  <done>Phase 2 can prove Trading Hub is selectable and persisted in the main gather UI while unsupported controls and unrelated field surfaces stay safe.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd //c "submacros\AutoHotkey64.exe /Validate /ErrorStdOut submacros\natro_macro.ahk" exits without syntax errors
- [ ] grep confirms Trading Hub guard logic exists in slot selection/unlock code
- [ ] grep confirms MFieldList still uses the original planter-safe fieldnamelist
- [ ] grep confirms settings/nm_config.ini still persists gather slots through FieldName1..3
</verification>

<success_criteria>

- All tasks completed
- Trading Hub selection disables unsupported slot controls safely
- Persisted reload behavior preserves the guarded Trading Hub slot state
- Main gather UI exposure stays separate from planter/manual field surfaces

</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-and-persistence/02-02-SUMMARY.md`
</output>
