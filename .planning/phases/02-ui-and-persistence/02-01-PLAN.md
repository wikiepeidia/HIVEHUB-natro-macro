---
phase: 02-ui-and-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - submacros/natro_macro.ahk
autonomous: true
requirements:
  - FIELD-01
  - FIELD-02
must_haves:
  truths:
    - Trading Hub is selectable in the main gather field dropdowns without appearing in planter/manual field registries.
    - Persisted and pasted gather-slot data can carry Trading Hub and Rejoin through the existing root config flow.
  artifacts:
    - path: submacros/natro_macro.ahk
      provides: Gather-only field exposure and persistence/import validation for Trading Hub.
  key_links:
    - from: submacros/natro_macro.ahk
      to: settings/nm_config.ini
      via: FieldName1..3 and CurrentFieldNum continue to persist through the existing gather config keys.
    - from: submacros/natro_macro.ahk
      to: nm_PasteGatherSettings
      via: gather-only field validation accepts Trading Hub and Rejoin.
---

<objective>
Expose Trading Hub in the main gather selectors and persistence/import flows without leaking it into planter/manual field registries.

Purpose: Phase 2 must make Trading Hub user-selectable and reloadable in the root UI while keeping shared non-gather field surfaces safe.
Output: A gather-only Trading Hub field list in root code and matching import/persistence validation updates in submacros/natro_macro.ahk.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-and-persistence/02-CONTEXT.md
@.planning/phases/02-ui-and-persistence/02-RESEARCH.md
@.planning/phases/02-ui-and-persistence/02-UI-SPEC.md
@submacros/natro_macro.ahk
@settings/nm_config.ini
@Hub file/submacros/natro_macro.ahk
</context>

<threat_model>
<trust_boundaries>
- `fieldnamelist` is shared by gather UI and planter/manual field surfaces.
- `settings/nm_config.ini` and clipboard-imported gather payloads feed back into active gather slot state.
</trust_boundaries>
<threat_register>
<threat>
<threat_id>T-02-01</threat_id>
<category>Tampering</category>
<component>Gather field exposure and persistence validation</component>
<severity>medium</severity>
<disposition>mitigate</disposition>
<mitigation_plan>Create a gather-only Trading Hub field list for the main gather UI and paste validation, keep planter/manual lists on the original fieldnamelist, and allow Rejoin through the same validation path.</mitigation_plan>
</threat>
</threat_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create a gather-only Trading Hub selector surface</name>
  <files>submacros/natro_macro.ahk</files>
  <read_first>submacros/natro_macro.ahk, Hub file/submacros/natro_macro.ahk, .planning/phases/02-ui-and-persistence/02-CONTEXT.md</read_first>
  <action>Define a dedicated gather-only field list in root code by cloning the existing `fieldnamelist` and appending `"Trading Hub"`. Rewire the `FieldName1..3` dropdowns to use that gather-only list. Keep `fieldnamelist` itself unchanged and keep `MFieldList := [""].Push(fieldnamelist*)` on the original non-Trading-Hub list so planter/manual field surfaces remain untouched.</action>
  <verify>cmd //c "submacros\AutoHotkey64.exe /Validate /ErrorStdOut submacros\natro_macro.ahk" && grep -n 'Trading Hub' submacros/natro_macro.ahk && grep -n 'MFieldList := \[""\]\.Push(fieldnamelist\*)' submacros/natro_macro.ahk</verify>
  <acceptance_criteria>
    - submacros/natro_macro.ahk defines a gather-only field list that includes Trading Hub
    - submacros/natro_macro.ahk uses the gather-only list for FieldName1, FieldName2, and FieldName3 dropdowns
    - submacros/natro_macro.ahk keeps MFieldList derived from the original fieldnamelist without Trading Hub
  </acceptance_criteria>
  <done>Trading Hub is available in the main gather selectors without entering planter/manual field registries.</done>
</task>

<task type="auto">
  <name>Task 2: Accept Trading Hub and Rejoin through the existing gather import path</name>
  <files>submacros/natro_macro.ahk</files>
  <read_first>submacros/natro_macro.ahk, settings/nm_config.ini, .planning/phases/02-ui-and-persistence/02-RESEARCH.md</read_first>
  <action>Update `nm_PasteGatherSettings()` so field-name validation uses the same gather-only list as the main gather dropdowns, not the shared planter-safe `fieldnamelist`. Expand the `ReturnType` validation regex to accept `Rejoin` in addition to `Walk` and `Reset`. Keep the existing `settings\nm_config.ini` key names (`FieldName1..3`, `FieldReturnType1..3`, `CurrentFieldNum`) unchanged so persisted reload behavior stays on the current root config flow.</action>
  <verify>cmd //c "submacros\AutoHotkey64.exe /Validate /ErrorStdOut submacros\natro_macro.ahk" && grep -n 'ObjHasValue(.*Trading Hub' submacros/natro_macro.ahk && grep -n 'ReturnType.*Walk|Reset|Rejoin' submacros/natro_macro.ahk</verify>
  <acceptance_criteria>
    - submacros/natro_macro.ahk validates pasted field names against the gather-only Trading Hub-capable list
    - submacros/natro_macro.ahk allows Rejoin in nm_PasteGatherSettings validation
    - submacros/natro_macro.ahk keeps the existing settings\nm_config.ini gather key names unchanged
  </acceptance_criteria>
  <done>Trading Hub selection and pasted gather settings can persist/reload through the root gather config flow without special-case storage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd //c "submacros\AutoHotkey64.exe /Validate /ErrorStdOut submacros\natro_macro.ahk" exits without syntax errors
- [ ] grep shows Trading Hub in the gather-only list and FieldName dropdown declarations
- [ ] grep confirms MFieldList still uses the original fieldnamelist
- [ ] grep confirms nm_PasteGatherSettings accepts Rejoin
</verification>

<success_criteria>

- All tasks completed
- Trading Hub appears in the main gather selectors
- Trading Hub and Rejoin survive the existing gather import/persistence path
- Planter/manual field registries remain Trading-Hub-free

</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-and-persistence/02-01-SUMMARY.md`
</output>
