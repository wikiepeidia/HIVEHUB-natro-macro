---
phase: 01-runtime-wiring
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - submacros/natro_macro.ahk
autonomous: true
requirements:
  - FLOW-03
  - FLOW-04
user_setup: []
must_haves:
  truths:
    - Trading Hub can leave the gather loop through an explicit supported return path instead of falling through to walk/reset behavior by accident.
    - Non-Trading-Hub fields keep their existing Walk and Reset behavior.
  artifacts:
    - path: submacros/natro_macro.ahk
      provides: Rejoin-aware return-type control and gather exit logic.
  key_links:
    - from: submacros/natro_macro.ahk
      to: Rejoin()
      via: explicit FieldReturnType="Rejoin" branch in the gather exit flow.
---

<objective>
Add Trading Hub-safe Rejoin return behavior in root code without widening shared UI exposure.

Purpose: Phase 1 must give Trading Hub a supported exit path so runtime flow does not break on walk-only assumptions or donor-only return behavior.
Output: Root return-type control supports Rejoin, and gather exit logic can call Rejoin() explicitly for Trading Hub runs.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-runtime-wiring/01-CONTEXT.md
@.planning/phases/01-runtime-wiring/01-RESEARCH.md
@submacros/natro_macro.ahk
@Hub file/submacros/natro_macro.ahk
</context>

<threat_model>
<trust_boundaries>

- Persisted gather return settings in settings/nm_config.ini cross into FieldReturnType1..3 and then into the gather exit branch.
- Runtime field identity in FieldName influences whether the macro walks back, resets, or rejoins.
</trust_boundaries>
<threat_register>
<threat>

<threat_id>T-01-02</threat_id>
<category>Denial of Service</category>
<component>Gather exit flow</component>
<severity>medium</severity>
<disposition>mitigate</disposition>
<mitigation_plan>Expand the return-type enum to include Rejoin, add an explicit FieldReturnType="Rejoin" branch that calls Rejoin(), and preserve current Walk/Reset logic for other fields.</mitigation_plan>
</threat>
</threat_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend the root return-type control to include Rejoin</name>
  <files>submacros/natro_macro.ahk</files>
  <read_first>submacros/natro_macro.ahk, Hub file/submacros/natro_macro.ahk</read_first>
  <action>In nm_FieldReturnType(GuiCtrl, *), change the static value list from ["Walk", "Reset"] to ["Walk", "Reset", "Rejoin"]. Keep the left/right cycling logic and the IniWrite FieldReturnType%index%, "settings\nm_config.ini", "Gather", "FieldReturnType" index line intact so existing return settings still persist normally. Do not add Trading Hub to fieldnamelist or the field dropdown widgets in this plan.</action>
  <verify>submacros/AutoHotkey64.exe /Validate /ErrorStdOut submacros/natro_macro.ahk</verify>
  <acceptance_criteria>
    - submacros/natro_macro.ahk contains static val := ["Walk", "Reset", "Rejoin"], l := val.Length
    - submacros/natro_macro.ahk still contains IniWrite FieldReturnType%index%, "settings\nm_config.ini", "Gather", "FieldReturnType" index
    - submacros/natro_macro.ahk does not add Trading Hub to fieldnamelist := [
  </acceptance_criteria>
  <done>The root return selector can persist Rejoin without changing shared UI field exposure.</done>
</task>

<task type="auto">
  <name>Task 2: Add an explicit Rejoin branch to the gather exit flow</name>
  <files>submacros/natro_macro.ahk</files>
  <read_first>submacros/natro_macro.ahk, Hub file/submacros/natro_macro.ahk</read_first>
  <action>In the gather exit flow near the existing FieldReturnType="walk" branch, add } else if(FieldReturnType="Rejoin") { Rejoin() } before the reset fallback so Trading Hub can use donor-style rejoin behavior. Preserve the existing Walk branch for normal fields. Keep the reset fallback for non-Rejoin cases. If any nearby whirligig or reset condition would still route Trading Hub through the normal walk or reset path, add the narrow FieldName!="Trading Hub" guard on that path rather than widening Trading Hub into other shared field systems.</action>
  <verify>submacros/AutoHotkey64.exe /Validate /ErrorStdOut submacros/natro_macro.ahk && grep -n 'FieldReturnType="Rejoin"\|Rejoin()\|FieldName!="Trading Hub"' submacros/natro_macro.ahk</verify>
  <acceptance_criteria>
    - submacros/natro_macro.ahk contains else if(FieldReturnType="Rejoin") {
    - submacros/natro_macro.ahk contains Rejoin() within the explicit Rejoin branch
    - submacros/natro_macro.ahk retains the existing else if(FieldReturnType="walk") branch for non-Trading-Hub fields
    - submacros/natro_macro.ahk contains FieldName!="Trading Hub" on any remaining whirligig or reset path that should not consume Trading Hub
  </acceptance_criteria>
  <done>Trading Hub has an explicit Rejoin-based exit path and no longer relies on donor-only or walk-only behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] submacros/AutoHotkey64.exe /Validate /ErrorStdOut submacros/natro_macro.ahk exits without syntax errors
- [ ] grep -n 'static val := \["Walk", "Reset", "Rejoin"\]' submacros/natro_macro.ahk confirms the expanded enum
- [ ] grep -n 'FieldReturnType="Rejoin"\|Rejoin()' submacros/natro_macro.ahk confirms the explicit Rejoin exit branch
</verification>

<success_criteria>

- All tasks completed
- Rejoin is available as a persisted return type in root code
- Gather exit logic can call Rejoin() explicitly for Trading Hub runs
- Existing Walk and Reset behavior remains intact for non-Trading-Hub fields

</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-wiring/01-02-SUMMARY.md`
</output>
