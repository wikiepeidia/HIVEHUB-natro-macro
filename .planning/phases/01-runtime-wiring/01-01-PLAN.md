---
phase: 01-runtime-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - submacros/natro_macro.ahk
  - settings/field_config.ini
autonomous: true
requirements:
  - FIELD-03
  - FLOW-01
  - SAFE-03
user_setup: []
must_haves:
  truths:
    - Root runtime imports the Trading Hub travel route and recognizes Trading Hub as a valid field without UI exposure.
    - Trading Hub has an explicit root-owned default profile instead of relying on donor runtime behavior.
  artifacts:
    - path: submacros/natro_macro.ahk
      provides: Trading Hub route import, field identity, booster metadata, and default map entries.
    - path: settings/field_config.ini
      provides: Persisted Trading Hub default section that matches the root default map.
  key_links:
    - from: submacros/natro_macro.ahk
      to: paths/gtf-tradinghub.ahk
      via: nm_importPaths path_names.gtf list includes tradinghub.
    - from: submacros/natro_macro.ahk
      to: settings/field_config.ini
      via: FieldDefault["Trading Hub"] is serialized into a [Trading Hub] INI section.
---

<objective>
Register Trading Hub in the root path, field, metadata, and default registries without exposing it in shared UI field lists.

Purpose: Make the existing root Trading Hub route and default profile available to runtime code so Phase 1 can satisfy FIELD-03, FLOW-01, and SAFE-03 before any UI work.
Output: Root Trading Hub registrations in submacros/natro_macro.ahk and a matching [Trading Hub] section in settings/field_config.ini.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-runtime-wiring/01-CONTEXT.md
@.planning/phases/01-runtime-wiring/01-RESEARCH.md
@submacros/natro_macro.ahk
@paths/gtf-tradinghub.ahk
@settings/field_config.ini
@Hub file/submacros/natro_macro.ahk
@Hub file/settings/field_config.ini
</context>

<threat_model>
<trust_boundaries>

- User-editable settings/field_config.ini is merged into FieldDefault inside submacros/natro_macro.ahk.
- Filesystem route content from paths/gtf-tradinghub.ahk is imported into paths["gtf"] through the hardcoded path_names map.
</trust_boundaries>
<threat_register>
<threat>

<threat_id>T-01-01</threat_id>
<category>Tampering</category>
<component>Trading Hub runtime registrations</component>
<severity>medium</severity>
<disposition>mitigate</disposition>
<mitigation_plan>Add explicit Trading Hub entries only in root path_names, fieldnames, FieldBooster, FieldDefault, and settings/field_config.ini; do not load behavior from Hub file at runtime; validate submacros/natro_macro.ahk after edits.</mitigation_plan>
</threat>
</threat_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Import the Trading Hub route and runtime field identity</name>
  <files>submacros/natro_macro.ahk</files>
  <read_first>submacros/natro_macro.ahk, paths/gtf-tradinghub.ahk, Hub file/submacros/natro_macro.ahk</read_first>
  <action>In nm_importPaths(), add tradinghub to the root gtf list so paths["gtf"]["tradinghub"] loads from paths\gtf-tradinghub.ahk. In the lower-case runtime fieldnames array, append tradinghub after coconut. Do not add tradinghub to gtp, wf, or fieldnamelist, and do not add Trading Hub to the main field dropdown lists in this phase because D-01 and D-04 keep shared UI exposure for Phase 2.</action>
  <verify>submacros/AutoHotkey64.exe /Validate /ErrorStdOut submacros/natro_macro.ahk</verify>
  <acceptance_criteria>
    - submacros/natro_macro.ahk contains "sunflower", "tradinghub"], ; go to field
    - submacros/natro_macro.ahk contains fieldnames:=["dandelion" and later "coconut", "tradinghub"]
    - submacros/natro_macro.ahk does not add Trading Hub to fieldnamelist := [
  </acceptance_criteria>
  <done>Trading Hub route import and lower-case runtime field identity exist in root code while shared UI-facing field lists remain untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add root-owned Trading Hub metadata and persisted defaults</name>
  <files>submacros/natro_macro.ahk, settings/field_config.ini</files>
  <read_first>submacros/natro_macro.ahk, settings/field_config.ini, Hub file/submacros/natro_macro.ahk, Hub file/settings/field_config.ini</read_first>
  <action>Add , "trading hub", {booster:"none", stacks:0} to FieldBooster. Add FieldDefault["Trading Hub"] := Map(...) with these exact values: pattern="Squares", size="M", width=5, camera="None", turns=1, sprinkler="Center", distance=1, percent=95, gathertime=300, convert="Rejoin", drift=0, shiftlock=0, invertFB=0, invertLR=0. Add a matching [Trading Hub] section to settings/field_config.ini with the same keys and values. Do not add FieldName1=Trading Hub or other active-slot values to settings/nm_config.ini in this phase.</action>
  <verify>submacros/AutoHotkey64.exe /Validate /ErrorStdOut submacros/natro_macro.ahk && grep -n '^\[Trading Hub\]$' settings/field_config.ini && grep -n '^convert=Rejoin$' settings/field_config.ini</verify>
  <acceptance_criteria>
    - submacros/natro_macro.ahk contains "trading hub", {booster:"none", stacks:0}
    - submacros/natro_macro.ahk contains FieldDefault["Trading Hub"] := Map("pattern", "Squares"
    - submacros/natro_macro.ahk contains , "gathertime",300 and , "convert","Rejoin" in the Trading Hub default map
    - settings/field_config.ini contains [Trading Hub]
    - settings/field_config.ini contains pattern=Squares, gathertime=300, and convert=Rejoin under the Trading Hub section
  </acceptance_criteria>
  <done>Root metadata and persisted defaults define Trading Hub explicitly and match the donor baseline without depending on Hub file at runtime.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] submacros/AutoHotkey64.exe /Validate /ErrorStdOut submacros/natro_macro.ahk exits without syntax errors
- [ ] grep -n 'tradinghub' submacros/natro_macro.ahk shows route import and lower-case runtime field entries
- [ ] grep -n '^\[Trading Hub\]$\|^pattern=Squares$\|^convert=Rejoin$' settings/field_config.ini confirms the persisted Trading Hub section
</verification>

<success_criteria>

- All tasks completed
- Trading Hub route import and default maps exist in root code
- settings/field_config.ini contains a root-owned Trading Hub section matching the code defaults
- Shared UI field lists remain unchanged for Phase 2

</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-wiring/01-01-SUMMARY.md`
</output>
