---
phase: 01-runtime-wiring
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - submacros/natro_macro.ahk
autonomous: true
requirements:
  - FIELD-03
  - FLOW-04
  - SAFE-03
user_setup: []
must_haves:
  truths:
    - Stale or partially migrated Trading Hub field defaults do not crash root config loading.
    - Root runtime validation can prove the Trading Hub slice is still root-owned and UI exposure remains deferred.
  artifacts:
    - path: submacros/natro_macro.ahk
      provides: Defensive Trading Hub config-load guard and root-only runtime validation surface.
  key_links:
    - from: settings/field_config.ini
      to: submacros/natro_macro.ahk
      via: nm_LoadFieldDefaults only merges known sections into FieldDefault.
---

<objective>
Harden Trading Hub config loading and verify the runtime-only boundary before execution starts.

Purpose: Phase 1 must survive stale Trading Hub config and prove that the runtime slice is root-owned without accidentally widening Trading Hub into the shared UI field surfaces reserved for Phase 2.
Output: Defensive Trading Hub default import logic in submacros/natro_macro.ahk and a repeatable validation checklist for runtime dispatch boundaries.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-runtime-wiring/01-CONTEXT.md
@.planning/phases/01-runtime-wiring/01-RESEARCH.md
@submacros/natro_macro.ahk
@settings/field_config.ini
@Hub file/submacros/natro_macro.ahk
</context>

<threat_model>
<trust_boundaries>

- settings/field_config.ini is user-editable persisted state and can contain stale or partially migrated sections.
- Runtime-only Phase 1 behavior must coexist with shared GUI surfaces that still exclude Trading Hub until Phase 2.
</trust_boundaries>
<threat_register>
<threat>

<threat_id>T-01-03</threat_id>
<category>Tampering</category>
<component>Field default import and runtime boundary</component>
<severity>medium</severity>
<disposition>mitigate</disposition>
<mitigation_plan>Ignore unknown field-default sections during nm_LoadFieldDefaults(), keep fieldnamelist unchanged, and verify the runtime wiring through grep plus AutoHotkey validation instead of widening UI lists in Phase 1.</mitigation_plan>
</threat>
</threat_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Harden field-default loading against stale Trading Hub config</name>
  <files>submacros/natro_macro.ahk</files>
  <read_first>submacros/natro_macro.ahk, settings/field_config.ini, .planning/phases/01-runtime-wiring/01-RESEARCH.md</read_first>
  <action>Update nm_LoadFieldDefaults() so it only writes FieldDefault[s][k] := ... when FieldDefault.Has(s) is true. Unknown sections should be ignored instead of indexing a missing map. Keep the existing rewrite behavior in nm_importFieldDefaults() so the root file still normalizes settings\field_config.ini after load. Do not add Trading Hub to fieldnamelist, MFieldList, or the GUI dropdown declarations in this plan.</action>
  <verify>submacros/AutoHotkey64.exe /Validate /ErrorStdOut submacros/natro_macro.ahk && grep -n 'FieldDefault.Has(s)' submacros/natro_macro.ahk</verify>
  <acceptance_criteria>
    - submacros/natro_macro.ahk contains FieldDefault.Has(s) inside nm_LoadFieldDefaults()
    - submacros/natro_macro.ahk still contains file := FileOpen(inipath, "w-d"), file.Write(ini), file.Close() in nm_importFieldDefaults()
    - submacros/natro_macro.ahk does not add Trading Hub to fieldnamelist := [
  </acceptance_criteria>
  <done>Unknown or stale Trading Hub sections cannot crash the field-default import path, and the Phase 1 runtime-only boundary remains intact in code.</done>
</task>

<task type="auto">
  <name>Task 2: Run root-only runtime validation checks for Trading Hub wiring</name>
  <files>submacros/natro_macro.ahk, settings/field_config.ini, paths/gtf-tradinghub.ahk</files>
  <read_first>submacros/natro_macro.ahk, settings/field_config.ini, paths/gtf-tradinghub.ahk, .planning/phases/01-runtime-wiring/01-RESEARCH.md</read_first>
  <action>Run the targeted validation commands that prove the Phase 1 boundary is correct: validate submacros/natro_macro.ahk with the bundled 64-bit AutoHotkey runtime; grep for tradinghub in path_names and fieldnames; grep for FieldDefault["Trading Hub"], FieldReturnType="Rejoin", and Rejoin(); grep settings/field_config.ini for [Trading Hub]; and confirm fieldnamelist := [ still does not contain Trading Hub. Record any failed assertion in the phase summary instead of broadening scope to fix Phase 2 UI work here.</action>
  <verify>submacros/AutoHotkey64.exe /Validate /ErrorStdOut submacros/natro_macro.ahk && grep -n 'tradinghub\|FieldDefault\["Trading Hub"\]\|FieldReturnType="Rejoin"\|Rejoin()\|fieldnamelist := \[' submacros/natro_macro.ahk && grep -n '^\[Trading Hub\]$' settings/field_config.ini</verify>
  <acceptance_criteria>
    - submacros/natro_macro.ahk contains tradinghub in the gtf path list and lower-case fieldnames list
    - submacros/natro_macro.ahk contains FieldDefault["Trading Hub"] and the explicit FieldReturnType="Rejoin" branch
    - settings/field_config.ini contains [Trading Hub]
    - submacros/natro_macro.ahk keeps fieldnamelist := [ free of Trading Hub
  </acceptance_criteria>
  <done>Phase 1 can prove the Trading Hub runtime slice is root-owned, config-safe, and still separated from Phase 2 UI exposure.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] submacros/AutoHotkey64.exe /Validate /ErrorStdOut submacros/natro_macro.ahk exits without syntax errors
- [ ] grep -n 'FieldDefault.Has(s)' submacros/natro_macro.ahk confirms the config-load guard
- [ ] grep -n 'fieldnamelist := \[' submacros/natro_macro.ahk | grep -v 'Trading Hub' confirms UI field-list exposure is still deferred
- [ ] grep -n '^\[Trading Hub\]$' settings/field_config.ini confirms persisted Trading Hub defaults remain present
</verification>

<success_criteria>

- All tasks completed
- Stale Trading Hub config cannot crash field-default import
- Root-only validation proves Trading Hub route, default, and Rejoin wiring are present
- fieldnamelist and shared UI field exposure remain deferred to Phase 2

</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-wiring/01-03-SUMMARY.md`
</output>
